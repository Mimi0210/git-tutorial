# Demo videos

Đặt video chuẩn hóa tại đây:

```text
01-cli.mp4
01-cursor-vscode.mp4
01-github-desktop.mp4
01-fork.mp4
… (02–09 tương tự)
```

Hiện một số clip đang nằm ở thư mục [`../video/`](../video/) và đã được map tạm vào web UI (CLI / clone). Sau khi quay đủ 4 tool, cập nhật đường dẫn trong `scripts/build-scenarios.js` rồi chạy:

```bash
npm run build:scenarios
```
